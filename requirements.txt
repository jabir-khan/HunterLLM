# Core pipeline (CPU OK)
httpx>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
typer>=0.12.0
rich>=13.0
tqdm>=4.66.0
trafilatura>=1.9.0
datasketch>=1.6.5
datasets>=2.18.0
# Fine-tuning (GPU; versions pinned loosely — align with your CUDA)
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
peft>=0.10.0
trl>=0.8.0
# Optional RAG (embedding index): numpy sentence-transformers
# Optional: pip install "unsloth[cu121] @ git+https://github.com/unslothai/unsloth.git"
