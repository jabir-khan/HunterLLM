# Core data pipeline (CPU OK on macOS)
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
typer>=0.12.0
rich>=13.0
tqdm>=4.66.0
trafilatura>=1.9.0
datasketch>=1.6.5
datasets>=2.18.0
huggingface_hub>=0.23.0

# Optional retrieval (CPU OK):
#   pip install numpy sentence-transformers
#
# Full training stack (CUDA Linux only):
#   pip install -e .[train]
# This installs: torch, transformers, accelerate, peft, trl,
# bitsandbytes, sentencepiece, wandb.
#
# Optional Unsloth (faster QLoRA):
#   pip install "unsloth @ git+https://github.com/unslothai/unsloth.git"
